   后台的Javascript可以调用所有的Java API，但是需要注意JavaScript的用法。

   后台的Javascript的内置变量：

   * douser:当前的登陆用户
   * doform: 当前的提交的表单内容
   * doinstance:当前记录

   内置变量用法：

   内置变量.getValue("属性名称") 就可以获取属性对应的值，如获取当前登陆者的部门: var deptuid = douser.getValue("deptuid");

   